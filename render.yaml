services:
  - type: web
    name: schoolmind-ui
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ui_server:app
    envVars:
      PYTHON_VERSION: 3.9.0
      PORT: 10000
      GROQ_API_KEY: ""
