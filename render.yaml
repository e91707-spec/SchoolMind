services:
  - type: web
    name: schoolmind-ui
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python ui_server.py
    envVars:
      PYTHON_VERSION: 3.9.0
      PORT: 10000
